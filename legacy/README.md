# Legacy Material

This folder keeps small references to the original WIL deliverables when they are safe to publish.

The original midterm Flask prototype contained a Google OAuth client secret, a hardcoded database connection, and a static encryption key, so that source file was not committed. The public version recreates the useful behavior with environment-variable configuration in the main application and scripts.

